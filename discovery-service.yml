eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: false
    fetchRegistry: false

server:
  port: ${DISCOVERY_PORT}

management:
  endpoints:
    web:
      exposure:
        include: "*"